/**
 * Number of milliseconds per frame for a frame rate of 60fps (16.6... ms)
 */
export const MS_PER_FRAME_60 = 1000 / 60;

/**
 * Number of milliseconds per frame for a frame rate of 20fps (50ms).
 */
export const MS_PER_FRAME_20 = 1000 / 20;
